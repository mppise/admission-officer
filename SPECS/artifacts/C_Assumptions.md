---
name: assumptions
description: Living register of all assumptions made during specification. Review and mark each as approved [X], rejected [-], deferred [>], or pending [ ].
license: Apache-2.0 (see LICENSE in project root)
---

# Assumptions

> An assumption left unreviewed is a hidden risk. This register must be reviewed before any dependent work begins.
>
> **Status codes:** `[ ]` Pending · `[X]` Approved · `[-]` Rejected · `[>]` Deferred

---

## Summary

| Total | Pending `[ ]` | Approved `[X]` | Rejected `[-]` | Deferred `[>]` |
| :---: | :-----------: | :------------: | :------------: | :------------: |
| 6 | 0 | 6 | 0 | 0 |

---

## Business & Product

| Status | ID | Assumption | Impact if Wrong | Owner | Notes |
| :----: | :- | :--------- | :-------------- | :---- | :---- |
| `[X]` | A-BP-AOIDEATE1 | Essay samples generated by `ao` are positioned as inspiration only and will not be used for submission directly by students | Tool could be misused for academic dishonesty; reputational and ethical risk | DevAgent | Must be surfaced clearly in CLI output and documentation |

## Users & Behavior

| Status | ID | Assumption | Impact if Wrong | Owner | Notes |
| :----: | :- | :--------- | :-------------- | :---- | :---- |
| `[X]` | A-UB-AOIDEATE1 | The student running `ao` is self-directed and technically comfortable using a CLI | Poor adoption; may need a wrapper or simpler UX | DevAgent | MVP targets CLI-comfortable students; no GUI planned |

## Technical

| Status | ID | Assumption | Impact if Wrong | Owner | Notes |
| :----: | :- | :--------- | :-------------- | :---- | :---- |
| `[X]` | A-TC-AOIDEATE1 | Google Gemini API will be used for guidance generation and essay outline/sample creation; API key and model name provided by user via `.env` file | If API key is invalid or quota exceeded, guidance and essay functions fail | DevAgent | Resolved during Ideation — confirmed by DevAgent |
| `[X]` | A-TC-AOIDEATE2 | University websites are publicly scrapable without authentication or bot-blocking that cannot be handled by standard Node.js scraping libraries | University profile builder fails for bot-protected sites | DevAgent | May need fallback to manual data entry wizard if scraping is blocked |
| `[X]` | A-TC-AOIDEATE3 | `GEMINI_API_KEY` and `GEMINI_MODEL` will be read from a `.env` file in the project root using `dotenv`; no other key management mechanism is needed for MVP | If key is missing or malformed, all LLM-dependent functions fail with a clear error | DevAgent | `.env` must be gitignored; user is responsible for key provisioning |

## External Dependencies & Integrations

| Status | ID | Assumption | Impact if Wrong | Owner | Notes |
| :----: | :- | :--------- | :-------------- | :---- | :---- |
| `[X]` | A-EX-AOIDEATE1 | PDF generation can be accomplished using a free Node.js library (e.g., `puppeteer`, `markdown-pdf`) without requiring a paid service | PDF export feature blocked or requires vendor spend | DevAgent | Library choice to be confirmed in Planning |

## Compliance & Security

| Status | ID | Assumption | Impact if Wrong | Owner | Notes |
| :----: | :- | :--------- | :-------------- | :---- | :---- |
| `[X]` | A-CS-AOIDEATE1 | All student data is stored locally on the student's machine; no cloud sync or remote storage is involved | Privacy risk if data is inadvertently transmitted; compliance concerns | DevAgent | Out-of-scope for MVP: cloud storage, auth, multi-user |

---

## Rejected Assumptions Log

| ID | Assumption | Rejected by | Date | Resolution |
| :- | :--------- | :---------- | :--: | :--------- |

---

## Change History

| ID | Description | Date | Author |
| :- | :---------- | :--: | :----- |
| CHG-001 | Initial assumptions seeded during Ideation | 2026-05-24 | SpecGantry |
